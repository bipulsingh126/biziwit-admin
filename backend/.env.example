# Server Configuration
PORT=4000

# Base URLs - Production Configuration
# Option 1: Same domain (requires reverse proxy)
BASE_URL=https://bizwitinsh.plenthia.com
FRONTEND_URL=https://bizwitinsh.plenthia.com
CORS_ORIGIN=https://bizwitinsh.plenthia.com

# Option 2: Separate API subdomain (recommended if no reverse proxy)
# BASE_URL=https://api.bizwitinsh.plenthia.com
# FRONTEND_URL=https://bizwitinsh.plenthia.com
# CORS_ORIGIN=https://bizwitinsh.plenthia.com

# Local Development (Commented for Production)
# BASE_URL=http://localhost:4000
# FRONTEND_URL=http://localhost:5173
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000,http://localhost:4000
# CLIENT_URL=http://localhost:5173

# MongoDB Configuration
# For MongoDB Atlas (Production), use this format:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/biziwit?retryWrites=true&w=majority
# 
# For Local MongoDB (Development), use this format:
# MONGODB_URI=mongodb://localhost:27017/biziwit
#
# IMPORTANT: Replace <username>, <password>, and cluster URL with your actual MongoDB Atlas credentials
# Example: mongodb+srv://admin:MyPassword123@cluster0.abc123.mongodb.net/biziwit?retryWrites=true&w=majority

MONGODB_URI=mongodb+srv://riyanshsingh123456789_db_user:OCTbou5rtX8OjDi3@cluster0.u1aj71l.mongodb.net/biziwit?retryWrites=true&w=majority

# Auth
JWT_SECRET=change_this_in_production
JWT_EXPIRES_IN=7d

# Seed (optional) - on startup will create a super admin if not exists
# Leave blank to disable seeding
ADMIN_EMAIL=superadmin@biziwit.com
ADMIN_PASSWORD=SuperAdmin@2024

# Uploads
UPLOAD_DIR=uploads

# Payments - Stripe
# Get these from Stripe dashboard
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Payments - PayPal
# PAYPAL_ENV can be 'sandbox' or 'live'
PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret

# Email (SMTP) for notifications
# If not set, email notifications are disabled
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
# Sender and recipient defaults
MAIL_FROM="BiziWit Admin <no-reply@biziwit.local>"
NOTIFY_EMAIL=alerts@yourdomain.com

# Mailchimp (optional newsletter sync)
# MAILCHIMP_SERVER_PREFIX is like 'us21' from your API key
MAILCHIMP_API_KEY=
MAILCHIMP_SERVER_PREFIX=
MAILCHIMP_LIST_ID=
